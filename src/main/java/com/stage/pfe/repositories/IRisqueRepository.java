package com.stage.pfe.repositories;

import com.stage.pfe.entities.Risque;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IRisqueRepository extends JpaRepository<Risque, Long> {
}
